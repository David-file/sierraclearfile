# Sierra Clearfile site

Public static site for Clearfile Menu FAQ and before/after sample images (GitHub Pages).

- FAQ: `/faq/`
- Samples: `/menu-samples/{slug}.jpg`

Custom domain (`sierraclearfile.com`) can be pointed here later.
